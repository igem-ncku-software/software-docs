# 測試測試
## 目前這裡應該沒有我們會用到的東西(?)

醫工的作業也是我隨便找一個jupyter當作測試

先試試看python
```python
print("iGEM!")
```

### 每次更新要做的事情
```bash
mkdocs build
git add .
git commit -m "Update manual"
git push
mkdocs gh-deploy --force
```